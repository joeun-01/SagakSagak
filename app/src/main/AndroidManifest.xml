<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.songilcraft.sagak_sagak">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_sagak"
        android:label="@string/app_name"
        android:name=".config.GlobalApplication"
        android:roundIcon="@mipmap/ic_sagak_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sagak_sagak">
        <activity
            android:name=".MainActivity"/>
        <activity android:name=".screen.start.StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screen.gallery.GalleryActivity"/>
        <activity android:name=".screen.storywrite.StoryWriteActivity"/>
    </application>

</manifest>